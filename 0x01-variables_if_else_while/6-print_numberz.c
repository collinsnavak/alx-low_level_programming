#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int interger;
for (interger = '0' ; interger <= '9' ; interger++)
{
putchar(interger);
}
putchar('\n');
return (0);
}
